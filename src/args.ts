export type Args = {
    showCurrentYear: boolean;
    monthsBefore: number;
    monthsAfter: number;
    width: number;
    month: number;
    year: number;
    showVersion: boolean;
    showHelp: boolean;
  };
  